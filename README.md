# 🏥 Multi-Hospital Bed Resource Management System

A robust, centralized dashboard for monitoring, managing, and optimizing hospital bed resources across multiple facilities. Built with modern web technologies, this system provides real-time insights, analytics, and streamlined resource allocation for healthcare networks.

## ✨ Key Features

- **Centralized Dashboard:** Monitor total capacity, occupied beds, and available resources in real-time across a network of hospitals.
- **Interactive Analytics:** Visual data representation using dynamic charts (Recharts) to track occupancy trends and department-wise bed availability.
- **Responsive Animations:** Fluid transitions and micro-interactions powered by Framer Motion for a premium user experience .
- **Secure Navigation:** Seamless client-side routing implemented with React Router DOM.
- **Modern UI Components  :** Accessible, beautifully designed components using Tailwind CSS and Headless UI.
- **Vector Icons:** Clean, scalable iconography using Lucide React.

## 🚀 Tech Stack

- **Frontend Framework:** React 18 with TypeScript
- **Build Tool:** Vite (for lightning-fast HMR and optimized builds)
- **Styling:** Tailwind CSS & Autoprefixer
- **UI Libraries:** Headless UI
- **Animations:** Framer Motion
- **Data Visualization:** Recharts
- **Icons:** Lucide-React
- **Routing:** React-Router-DOM
- **Code Quality:** ESLint, TypeScript Strict Mode

## 🛠️ Installation & Setup

Follow these steps to get the project up and running on your local machine:

1. **Clone the repository:**
   ```bash
   git clone https://github.com/ABHISHEK27Y/Multi-Hospital-Bed-Resource-Management-System-.git
   ```

2. **Navigate into the project directory:**
   ```bash
   cd Multi-Hospital-Bed-Resource-Management-System-
   ```

3. **Install Dependencies:**
   Make sure you have [Node.js](https://nodejs.org/) installed, then run:
   ```bash
   npm install
   ```

4. **Start the Development Server:**
   ```bash
   npm run dev
   ```

5. **Open in Browser:**
   Once the server starts, open the provided localhost link (typically `http://localhost:5173`) in your browser to view the application.

## 📁 Project Structure

```text
├── src/
│   ├── components/       # Reusable UI components (Cards, Charts, Layouts)
│   ├── pages/            # Main application views (Dashboard, Facilities, etc.)
│   ├── App.tsx           # Main App layout and Route configurations
│   ├── main.tsx          # React application entry point
│   └── index.css         # Global Tailwind directives and custom styles
├── public/               # Static assets
├── package.json          # Project metadata and dependency tree
├── tailwind.config.js    # Tailwind configuration and theme customization
└── vite.config.ts        # Vite build tool configuration
```

## ⚙️ Available Scripts

- `npm run dev`: Starts the Vite development server.
- `npm run build`: Compiles TypeScript and builds the app for production in the `dist/` folder.
- `npm run lint`: Runs ESLint to check for code quality and errors.
- `npm run preview`: Boot up a local static web server that serves the files from the `dist/` folder.

## 🤝 Contributing

Contributions, issues, and feature requests are welcome! 
Feel free to check out the [issues page](https://github.com/ABHISHEK27Y/Multi-Hospital-Bed-Resource-Management-System-/issues).

## 📝 License

This project is proprietary/open-source. Please check the repository for specific licensing details.

---
*Developed with 💙 by [ABHISHEK27Y](https://github.com/ABHISHEK27Y)*
